version: '3'
services:
  back:
    image: henridrg/back
    ports:
      - "5000"
    environment:
      - CURRENT_ENVIRONMENT=production
#    volumes:
#      - ./logs:/app/logs
  front:
    image: henridrg/front
    ports:
      - "8083:80"
    environment:
      - TR_WS_BACK_URL=http://localhost:8080
